{
  "name": "timer",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "npm install && bower install && gulp"
  },
  "dependencies": {
    "body-parser": "~1.12.4",
    "cookie-parser": "~1.3.5",
    "debug": "~2.2.0"
  },
  "devDependencies": {
    "gulp": "^3.9.0",
    "main-bower-files": "^2.9.0"
  }
}
